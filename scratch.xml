<?xml version="1.0" encoding="UTF-8"?>
<?asciidoc-toc?>
<?asciidoc-numbered?>
<article xmlns="http://docbook.org/ns/docbook" xmlns:xl="http://www.w3.org/1999/xlink" version="5.0" xml:lang="en">
<info>
<title>Scratch</title>
<date>2020-04-26</date>
</info>
<section xml:id="_random">
<title>Random</title>
<literallayout class="monospaced"> ★☡oʀʀoъ★
☡øʀʀø★</literallayout>
<section xml:id="_bspwm">
<title>bspwm</title>
<simpara>bspc query -N -n .floating | xargs xtitle -f "%s==bspc node %u -n pointed" | dmenu -l 5 -d "==" | sh
bspc node -t fullscreen</simpara>
</section>
<section xml:id="_c_lang">
<title>C lang</title>
<simpara>Read hole file in C</simpara>
<programlisting language="c" linenumbering="unnumbered">long length;
FILE * f = fopen (filename, "rb");

if (f)
{
  fseek (f, 0, SEEK_END);
  length = ftell (f);
  fseek (f, 0, SEEK_SET);
  buffer = malloc (length);
  if (buffer)
  {
    fread (buffer, 1, length, f);
  }
  fclose (f);
}

if (buffer)
{
  // start to process your data / extract strings here...
}</programlisting>
</section>
<section xml:id="_freebsd">
<title>FreeBSD</title>
<simpara>CDPATH environment variable
The CDPATH environment variable contains a colon-separated list of directories that will be searched for the argument of a cd command, if the argument does not begin with a slash.
pkg add ~/Builds/abiword/abiwords-3.0.2.txz
Temperature
	sysctl -a | grep -i temperature
	ipmitool sensor</simpara>
</section>
</section>
<section xml:id="_tail">
<title>Tail</title>
<simpara>Default XDG application xdg-mime default xfe.desktop inode/directory
XOSD displays text on your screen, sounds simple right? The difference is it is unmanaged and shaped, so it appears transparent. This gives the effect of an On Screen Display, like your TV/VCR etc. Comes with an XMMS plugin.
sixel image in terminal format
vim template/snippet - input read iabbrev
set complete+=k
set dictionary+=/your/dict/file
pdfgrep
qualculate
котейнеризация linux namespaces cgroups
CP2615 usb-I2S
вейвлеты Wavelet Tree Для установки k-го бита array[k/8] |= 1 &lt;&lt; (k % 8)
TTS flite festival espeak
simplenote nvpy
service nfsclient start
mount yept:/data /mnt
umount /mnt</simpara>
<simpara>doas cu -s 115200 -l /dev/ttyu0</simpara>
<simpara>амлодипин</simpara>
<simpara>Message from atop-2.0.2.b3_3:</simpara>
<simpara>atop succesfully installed. To run in interactive mode use "atop" command.</simpara>
<simpara>To run the atop in collecting mode from startup, add</simpara>
<simpara>atop_enable="YES"</simpara>
<simpara>in your /etc/rc.conf.</simpara>
<simpara>You will also need to add "rotate" command to the system crontab:</simpara>
<simpara>0 0 * * *               /usr/local/etc/rc.d/atop rotate &gt;/dev/null</simpara>
<simpara>To change number of days to keep the logs (default - 30) use atop_keepdays
variable in your /etc/rc.conf file.</simpara>
<simpara>To change default (10 seconds) interval use atop_inteval (e.g. atop_interval=5)
in /etc/rc.conf</simpara>
<simpara>татарское приветствие "исянмесез" буквально переводится как "живы ли вы?"</simpara>
</section>
</article>